version: "2"
sql:
  - engine: "postgresql"
    schema: "db/schema"
    queries: "db/query"
    gen:
      go:
        package: "db"
        out: "internal/db"
        sql_package: "pgx/v5"
        emit_json_tags: true
